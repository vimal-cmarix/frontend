import styled from 'styled-components';
import {
  Title as TitleStyle,
  Resume as ResumeStyle,
} from '@pages/pricing/style';

export const Title = styled(TitleStyle)`
  margin-top: 30px;
  margin-bottom: 24px;
`;

export const Resume = styled(ResumeStyle)``;
