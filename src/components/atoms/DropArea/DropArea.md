<h3> Drop Area - Examples </h3>

- Primary

```js
<DropArea
  onPick={() => {}}
  description="Accepts all major video file types."
  maxSize={50 * 1024 * 1024}
/>
```

- Secondary

```js
<DropArea
  onPick={() => {}}
  description="Accepts all major video file types."
  maxSize={50 * 1024 * 1024}
  colorSchema="secondary"
/>
```
